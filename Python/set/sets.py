#how to make empty set 
 #e = set() # dont use s = {} because it will create...dictnory 
#print(type(e))
s= set()
s = {32, 44 , 1 ,2 , "harry" } # set does not repeat elements but set does not mantain order
print(s) 
print(type(s))
# Unordered collection.
# Stores only unique elements (no duplicates).
# Mutable (can add or remove elements).
# Elements must be immutable (hashable).
# No indexing or slicing.
